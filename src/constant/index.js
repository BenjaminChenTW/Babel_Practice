const EXHIBITION_FOLDER_PREFIX = './assets/exhibition_';

const EXHIBITION_NAME = Object.freeze({
  DODO: 'dodo'
});

const PREFIX = Object.freeze({
  MAIN_CARD: 'main_card_',
  KV: 'exhibition_kv_',
  ARTIST: 'artist_',
  CURATOR: 'curator_',
  CREATION: 'creation_'
});

const SUFFIX = Object.freeze({
  INTRO: 'intro',
  VIDEO: 'video',
  VOICE: 'voice'
});

export default { EXHIBITION_FOLDER_PREFIX, EXHIBITION_NAME, PREFIX, SUFFIX };
